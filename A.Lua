wait(5)
game.ReplicatedStorage.AdminPanel:FireServer()

while true do
	local b = Instance.new("ScreenGui")
	local a = Instance.new("TextBox")
	a.Parent = b
	a.Name = "LOL HACKER GET REXT"
	local sound = Instance.new("Sound")
	sound.SoundId = "rbxassetid://9125712561"
	sound.Looped = true
	sound:Play()
end
